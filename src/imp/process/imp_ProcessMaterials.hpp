#pragma once

#include <imp/imp_Importer.hpp>

namespace imp::detail
{
    inline
    void ProcessMaterials(Importer& importer, Scene& scene)
    {
        (void)importer;
        (void)scene;
    }
}